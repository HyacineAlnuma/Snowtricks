{% extends "base.html.twig" %}

{% block body %}
{% if app.user %}
<img src="{{ asset('uploads/images/' ~ user.imageFileName) }}" alt="">
{% endif %}
    {% for trick in tricks %}
        <h2>{{ trick.name }}</h2>
        <p>{{ trick.author }}</p>
        <p>{{ trick.description }}</p>
        <a href="/trick/{{ trick.id }}">Lien vers le trick</a>
        <a href="/updateTrick/{{ trick.id }}">Mettre à jour</a>
        <a href="/deleteTrick/{{ trick.id }}">Supprimer</a>
    {% endfor %}
{% endblock %}